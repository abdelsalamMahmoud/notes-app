<?php 
    require "../views/index.view.php";
    // require "../Database.php";
    
    // $config = require "../config.php";

    // $db = new Database($config['database'],'root','abdelsalam30');

    // $id = $_GET['ID'];

    // $query = "select * from posts where ID = ?";

    // $post =  $db->query($query,[$id])->fetch();//fetchAll to return all records

    // dd($post['title']);